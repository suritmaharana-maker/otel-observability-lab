# Cilium 1.19.4 Helm values for AWS EKS ENI mode
# Source: docs.cilium.io/en/stable/installation/k8s-install-helm/
#
# ENI mode: Cilium manages ENIs and IP allocation in place of AWS VPC CNI.
# aws-node DaemonSet is patched with a non-matching nodeSelector before this
# installs — preventing ENI management conflicts.

# ── ENI / IPAM ────────────────────────────────────────────────────────────────
eni:
  enabled: true

ipam:
  mode: eni

# Native routing — no overlay encapsulation, uses VPC routing directly
routingMode: native

# Required for ENI mode
bpf:
  masquerade: true

# ── NODE TAINT MANAGEMENT ─────────────────────────────────────────────────────
# Cilium removes node.cilium.io/agent-not-ready taint automatically
# once it has fully initialised on each node.
removeNodeTaints: true
setNodeTaints: true

# ── KUBE PROXY REPLACEMENT ───────────────────────────────────────────────────
# false = safe default for EKS; kube-proxy addon handles service routing
kubeProxyReplacement: false

# ── OPERATOR ─────────────────────────────────────────────────────────────────
operator:
  replicas: 1
  # Disable aggressive restart of unmanaged pods (e.g. CoreDNS).
  # Source: medium.com/@pavankumarn6997 — prevents CoreDNS restart loops
  # when Cilium operator starts before CoreDNS has Cilium networking.
  unmanagedPodWatcher:
    restart: false

# ── HUBBLE — NETWORK OBSERVABILITY ───────────────────────────────────────────
# Enabled now so Phase 2 instrumentation requires no cluster changes.
hubble:
  enabled: true
  relay:
    enabled: true
  ui:
    enabled: false
  metrics:
    enabled:
      - dns
      - drop
      - tcp
      - flow
      - port-distribution
      - icmp
      - httpV2:exemplars=true;labelsContext=source_ip,source_namespace,source_workload,destination_ip,destination_namespace,destination_workload,traffic_direction
    serviceMonitor:
      enabled: false

# ── RESOURCES ─────────────────────────────────────────────────────────────────
# Sized for t3.xlarge nodes (4 vCPU, 16GB RAM)
resources:
  limits:
    cpu: 500m
    memory: 512Mi
  requests:
    cpu: 100m
    memory: 128Mi
